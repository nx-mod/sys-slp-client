# sys-slp-client — slp relay server list (max 10)
#
# Format (one server per line, '#' starts a comment):
#     <name> <host:port>      or      <name> <host> <port>
#
# This file is read by:
#   - the sys-slp-client sysmodule  (on-device: /config/slp-helper/servers.conf)
#   - the slp-helper Tesla overlay  (same path)
# Keep names short; they appear in the overlay picker.
#
# Stats below are snapshot values from the slp status site (2026-08-13).

local     10.236.81.153:11451
#          local hotspot relay (slp-server-rust on this device)

tekn0      tekn0.net:11451
#           live server online